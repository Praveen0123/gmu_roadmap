import { SortByTypesPipe } from './sort-by-types.pipe';

describe('SortByTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
